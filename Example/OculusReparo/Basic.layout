table {
    position {
        height: 100%;
        width: 100%;
    }

    id: table;
    set-delegate;

    cells {
        @each: items {
            cell {
                text: @item.text;
                accessory: disclosure-indicator;
            }
        }
    }
}